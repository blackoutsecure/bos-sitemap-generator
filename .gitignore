# Dependencies
# package-lock.json is intentionally committed: GitHub Marketplace Actions need
# a reproducible npm install in CI (`npm ci`), which requires the lockfile.
# Dependencies
# package-lock.json is intentionally committed: GitHub Marketplace Actions need
# a reproducible npm install in CI (`npm ci`), which requires the lockfile.

# Test artifacts (generated by test suite, product-specific)
test/output/
dist/sitemap*.xml
dist/sitemap*.txt
dist/sitemap*.xml.gz

# Temporary files
*.temp


# >>> managed-file-sync:common >>>
# OS + editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/*
!.vscode/extensions.json

# Build + dependency output
node_modules/
dist/
build/
coverage/
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Local secrets (never commit these)
.env
.env.*
!.env.example
# <<< managed-file-sync:common <<<

# >>> managed-file-sync:coverage_artifacts >>>
.coverage
.coverage.*
coverage.xml
htmlcov/
# <<< managed-file-sync:coverage_artifacts <<<
